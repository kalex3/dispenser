services:
  dispenser:
    build: .
    image: dispenser:latest
    container_name: dispenser
    restart: unless-stopped
    # ports:
    #   - 3000:3000
    # environment:
    #   - NODE_ENV=production
    #   - HOST=0.0.0.0
    #   - PORT=3000
    # volumes:
    #   - ./resources/accounts.txt:/app/resources/accounts.txt:ro
    #   - ./resources/blocked_ips.txt:/app/resources/blocked_ips.txt:ro
    #   - ./logs:/app/logs
